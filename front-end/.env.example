# ── API Base URL ──────────────────────────────────────────────────────────────
# Local development: leave blank — Vite proxies /api → localhost:4000
# Production (EC2): set to your server's public IP or domain
VITE_API_BASE_URL=
